<% if @posts %>
    <% @posts.each do |pst| %>
        <%= pst.body %>
    <% end %>
<% end %>

<h1>No posts yet!</h1>
<%= current_user.id %>
<%= button_to "Log Out", destroy_user_session_path, method: :delete, class:"logoutButton" %>