<%= stylesheet_link_tag "root_page" %>
<h2 class="loginTitle">“Yokoso Watashi no Soul society”</h2>
<div class="loginContainer">

  <div class="loginForm">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

  <div class="nameField">
      <%= f.text_field :name, autofocus: true, autocomplete: "username",placeholder: "username" %>
    </div>

    <div class="emailField">
      <%= f.email_field :email, autofocus: true, autocomplete: "email",placeholder: "email" %>
    </div>

    <div class="passwordField">
      <%= f.password_field :password, autocomplete: "current-password",placeholder:"password" %>
    </div>

    <div class="loginAndcheckbox">
      <% if devise_mapping.rememberable? %>
        <div class="checkboxField">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>

      <div class="actionsField">
        <%= f.submit "Log in",class:"loginButton" %>
      </div>
    </div>
  <% end %>

  <div class="shrLinks">
    <%= render "devise/shared/links" %>
  </div>
</div>  